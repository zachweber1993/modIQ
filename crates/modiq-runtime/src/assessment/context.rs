/// Represents the runtime context for an Assessment.
///
/// The context describes the environment and configuration
/// under which an assessment is performed. It does not
/// contain assessment state or assessment results.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct AssessmentContext;
